[project]
name = "griptape-nodes-library-corridorkey"
version = "0.3.0"
description = "Griptape Nodes library wrapping the CorridorKey neural green/blue screen keying model."
authors = [{ name = "Griptape, Inc.", email = "hello@griptape.ai" }]
readme = "README.md"
requires-python = "~=3.12.0"
# The edit-time set, and nothing more. The execution set is declared only in
# pip_dependencies_exec: the engine installs that additively with `uv pip install`, so it
# never has to co-resolve with the engine's own closure. An extra here would, and one
# incompatible pin anywhere in it would then fail `uv lock` for the whole project.
dependencies = [
    "numpy",
    "Pillow",
]

[tool.uv.sources]
griptape-nodes-engine = { git = "https://github.com/griptape-ai/griptape-nodes-engine", rev = "latest" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["griptape_nodes_library_corridorkey"]

[dependency-groups]
dev = ["pyright>=1.1.396", "pytest-asyncio>=0.21.0", "ruff>=0.8.0", "pytest", "griptape-nodes-engine"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
  ".venv",
  "**/node_modules",
  "**/__pycache__",
  "griptape_nodes_library_corridorkey/CorridorKey",
]

[tool.ruff.lint]
select = [
  "E",  # pycodestyle errors
  "W",  # pycodestyle warnings
  "F",  # Pyflakes
  "I",  # isort
  "B",  # flake8-bugbear
  "C4", # flake8-comprehensions
  "UP", # pyupgrade
]
ignore = [
  "E501", # line too long (handled by formatter)
  "B008", # do not perform function calls in argument defaults
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["."]
exclude = [".venv", "**/node_modules", "**/__pycache__", "**/.*", "templates", "griptape_nodes_library_corridorkey/CorridorKey"]
pythonVersion = "3.12"
reportIncompatibleMethodOverride = false
reportMissingImports = false
reportMissingModuleSource = false
